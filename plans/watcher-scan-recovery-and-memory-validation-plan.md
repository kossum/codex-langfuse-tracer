# Watcher scan recovery and memory validation

- Date: 2026-09-22
- Status: W0-W5 complete; W4's candidate/baseline worker matrix passed under an explicit user waiver of the host-pressure limit; W6 runtime is installed and the automatic Codex canary passed, but post-install host acceptance was stopped early after severe PSI/reserve breaches and repeated OOM kills of the watcher. The production watcher was left running at the user's direction; latest check had state v3 processed=1,067, queue=0, pending=0 and `NRestarts=14`. A fresh 60-minute acceptance window must wait until host pressure recovers.
- Starting source: `14c656ff5462ae43a65648a2ffa6c5d8f84647ad`
- Owner: `codex-langfuse-tracer`
- Executor: a coding agent, including one with a smaller context window
- Predecessor: [delivery reliability plan](export-delivery-reliability-plan.md)
- Operational record: [canonical handoff](multi-machine-tracing-gateway-handoff.md)

## 1. Outcome and evidence boundary

Make Codex discovery, stat, and parse failures consistently retryable and visible; avoid repeatedly parsing unchanged healthy files while a failure holds the scan watermark; replace the weak large-input probe with tests that prove which input was consumed; and measure the supported workload before accepting a resource budget.

The historical duplicate incident remains explained by accepted exports followed by failed local checkpoints and watcher restarts. Advisory locking and delivery diagnostics are already deployed. This follow-up does not reopen that RCA or promise exactly-once delivery.

The prior streaming change remains useful. It removes whole-file string/split copies and filters processed turns before retaining observations. It does **not** establish constant memory: `ReadBytes('\n')` and JSON decoding allocate for an entire record, and all selected turns and tool associations remain in memory until parsing finishes.

The 60-minute service observation and automatic Codex canary are valid historical observations. They do not supply a workload-derived host capacity budget. The later substitution of a 4 GiB `MemAvailable` threshold for the original memory/swap recovery condition was not justified before testing. Do not treat that substitution as a passed gate.

### Findings to preserve

| Finding | Evidence at the starting source | Required correction |
| --- | --- | --- |
| Stat errors can lose eligibility | `ScanOnce` continues after `os.Stat` errors without setting `scanFailed`; a later watermark can exceed a restored file's unchanged mtime | Hold progress on any uncertain stat result and test recovery at the old mtime |
| Discovery hides failures and can omit nested files | `SessionPaths` ignores walk errors and uses a one-directory glob before recursive fallback; a shallow match suppresses deeper discovery | One recursive discovery implementation returning partial paths plus errors |
| Parse failure causes repeated work | Corrupt input pins the global watermark; every eligible healthy source is parsed again every poll | Preserve conservative progress, with a disposable cache of successful unchanged scans |
| Doctor misses incomplete scans | Parser/stat messages are warnings, suppressed by quiet mode; `recentErrorCount` recognizes `ERROR:` | Emit an unconditional incomplete-scan error summary; retain documented recent-error semantics |
| Large probe can pass without reading its target | It checks only watermark advancement; its live symlink can become newer than the artificial scan time | Stable private input, positive target-consumption assertions, and deliberate negative controls |
| Probe counts and memory claims are overstated | `processed_traces` is the size of the entire loaded state; RSS is externally observed, with no budget assertion | Report actual source/state overlap and measure isolated workers against criteria fixed first |
| Existing functional filter tests cannot prove retention behavior | Their final output would also pass if all turns were parsed and filtered later | Add a mechanism assertion and independent resource measurements |

The preceding audit reproduced the stat, corrupt-input, and vacuous-probe cases with temporary synthetic sources. Those examples are diagnostic evidence, not registered regression tests. W1 must turn them into executable regressions.

## 2. Fixed design decisions

1. Keep state version 3, its single state file, persistent advisory sidecar, and read/mutate/atomic-write transaction. No schema migration is needed.
2. Keep at-least-once sending and existing per-turn checkpoints. Never mark a trace processed before successful span/score work. Retry a pending score without resending spans.
3. Keep one global discovery watermark. Any discovery, stat, parse, or ordinary per-turn delivery failure prevents its advancement for that scan. Successful turns may still commit individually. Continue healthy work after a recoverable source failure.
4. Use process-local metadata only to suppress redundant parsing. It is an optimization, never a second progress store. Restart, cache eviction, or cache invalidation may cause rereading; none may cause a turn to be marked processed or skipped permanently.
5. Keep `ScanOnce` as a one-shot entry point. Add a private scan function and private runtime object shared across `WatchSessions` iterations. Unit tests in package `watch` can exercise that private interface. Do not introduce public CLI flags for test injection.
6. Report incomplete scans as failures in diagnostics even with `Quiet=true`, without returning an ordinary source error from the watch loop and causing a restart. State I/O/checkpoint errors and cancellation retain their existing fatal/cancellation behavior.
7. Do not repurpose the Claude hook queue as a Codex discovery retry ledger. Its current parser is unfiltered, and successful empty/non-exportable transcripts do not drain it. Changing that contract is unnecessary here.
8. Do not cap, truncate, discard, or mark large input processed to meet memory targets. Do not flush a turn merely because the next `turn_context` appeared: the parser supports revisited turn IDs and tool associations.
9. Preserve the existing fixture inventory and normalized projection. New checked-in source/golden fixtures belong in `testdata/manifest.json`. Large synthetic inputs are generated privately at test time, not committed as another fixture corpus.
10. Keep Claude hooks, pricing, gateway/reconciliation, export transport, manual export scope, and receiver deduplication outside this change. Shared state/hook regression tests remain required.

### Deliberate limits of the selected recovery design

This remains an mtime-based scanner of currently discoverable sources, not a durable archive of every previously seen pathname. A discoverable source whose stat/read fails must remain eligible when access recovers. A failed discovery must not certify a complete scan. A corrupt eligible source remains degraded until it is repaired or deliberately removed from the watched source tree by its owner.

A file deleted from discovery and later restored with an mtime older than an already successful watermark is outside the existing automatic discovery guarantee. So are arbitrary rewrites that preserve file identity, size, and mtime. This plan must not claim to repair those cases, and the cache must not broaden that limitation. Supporting them would require a separate discovery/reconciliation contract. Do not hide this limit behind an in-memory list that disappears at restart.

A missing `sessions/` directory is conservatively an incomplete Codex discovery, including on a fresh host. Keep the old watermark and continue draining the existing hook queue. Doctor may report degraded Codex discovery on a host where Codex has not created its directory; explain the cause instead of resetting progress or inventing an exclusion flag.

### Why this design

| Alternative | Decision |
| --- | --- |
| Advance the watermark after a warning | Reject: loses retry eligibility |
| Pin the watermark and reparse everything forever | Correct conservative progress, but unnecessary repeated allocation/CPU; add the disposable cache |
| Persist failed paths and advance a global watermark | Defer: requires a versioned durable retry contract, removal semantics, and compatibility with all state writers/rollback |
| Use the existing hook queue for every Codex failure | Reject for this scope: changes queue completion, filtering, and eligibility semantics |
| Drop corrupt/oversized input after N tries | Reject: silent data loss |
| Rewrite the whole parser into a turn iterator now | Defer unless measured budgets fail; interleaved contexts and call matching require a separate semantic design |

## 3. File map

Read these before editing. Locate symbols with `rg`; line numbers will move.

| File | Role / allowed work |
| --- | --- |
| `internal/codextrace/sessions.go` | Recursive discovery, error propagation, `FindSessionByID`, `LatestSession` |
| `internal/codextrace/parser.go` | Preserve filtering/projection; extract a private reader/helper only when needed for a real mechanism test |
| `internal/codextrace/parser_test.go` | Output equivalence, processed-history filtering, interleaved contexts |
| `internal/watch/watch.go` | `ScanOnce`, private repeated-scan runtime, failure aggregation, stable-file cache |
| `internal/watch/watch_test.go` | Watermark, eligibility, recovery, pending-score, quiet and lifecycle tests |
| `internal/watch/large_rollout_live_test.go` | Repair real-source probe; never touch production input/state |
| `internal/watch/watch_delivery*_test.go`, `watch_lock_unix_test.go` | Preserve lost-ack, process death, contention and checkpoint coverage |
| `cmd/codex-langfuse-exporter/main.go` and tests | Doctor incomplete-scan visibility and unavailable-journal handling |
| `internal/exportstate/`, `internal/claudehook/` | Read contracts and run regressions; no schema/queue redesign |
| `test/` | Existing documentation, install and normalized-contract tests |
| `TESTING.md`, `README.md` | Executable verification commands and user-visible troubleshooting |
| This plan, predecessor and canonical handoff | Decisions, actual results, limitations and deployment record |

## 4. Phase sequence

Finish each phase's exit criteria before checking it off. For each phase record source revision, commands, real result, failure/rework, risk, and the next action. Names below are **proposed tests**, not evidence that those tests currently exist or pass.

### W0 — Baseline and acceptance protocol

1. Read `AGENTS.md`, `README.md`, `TESTING.md`, this plan, and `plans/performance-test-stability.md`. Inspect `git status --short --branch`; preserve unrelated changes.
2. Record current source, installed build revision, running executable digest, active/enabled service status and restart counter. Do not assume the dated runtime snapshot is current. Read only aggregate state counts/version and host memory/PSI/paging counters; do not print credentials, rollout text or the full state.
3. Run `go test ./... -count=1` and `git diff --check`. Classify an existing failure before editing tests. Preserve assertions and production contracts; never remove a test to make the baseline green.
4. Confirm the predecessor and handoff contain the correction notice. Preserve the historical 60-minute/canary observations and their exact limits. Mark the previous 41,408 KiB result as a measurement of an invocation whose target consumption was not positively proven.
5. Freeze the following local host protocol before candidate resource measurements. One watcher instance is installed on this host. At 2026-09-22 17:38 PDT, its systemd cgroup reported `MemoryPeak=544,874,496` bytes (519.7 MiB) and current memory 145.8 MiB. The largest sampled Codex process used 2,057,832 KiB (1.96 GiB); the host has 30.5 GiB RAM. Set both the isolated scan worker's hard cgroup `MemoryMax` and measured process-RSS allowance to 768 MiB, about 1.48 times the observed installed watcher peak. This gives roughly 48% growth allowance while keeping the worker below the available headroom. Disable swap for each worker. Keep at least 2 GiB `MemAvailable` during deployment observation, matching/rounding up the observed largest active Codex process. Candidate limit and its arithmetic were written before W4 measurements.
6. Record 2026-09-22 17:38 PDT host baseline: `MemAvailable=3,828,277,248` bytes (3.57 GiB), swap free 81,920 bytes of 8 GiB, memory PSI `some avg10=0.01`, `full avg10=0.00`, `some avg60=0.13`, `full avg60=0.11`. Active workloads included Codex, Next.js and ClickHouse processes. Host acceptance samples every minute for 60 minutes: no new watcher restart or kernel OOM; `MemAvailable >= 2 GiB`; PSI `some avg10 <= 5.0` and `full avg10 <= 1.0` at each sample; active/running/enabled watcher; report swap-in/out counter deltas. Full swap is a recorded condition, not a standalone failure; any OOM, breach of the fixed reserve/PSI limits, or unexplained sustained paging/pressure blocks host acceptance. These thresholds are tied to this host's observed largest active process and available memory, and are fixed before candidate measurements. If deployment occurs under a materially different workload or on another host, refresh this protocol before measuring there.

**Exit:** baseline known; acceptance methodology written; any numerical resource/host limits still unfilled are explicit release blockers, not discretionary defaults for the executor to invent.

### W1 — Register reproductions and repair the probe

1. Add deterministic failing regressions for stat recovery, discovery failure, mixed-depth discovery, repeated parsing behind a corrupt source, and quiet-mode health visibility. Use injected filesystem/discovery errors rather than `chmod` assumptions that can pass when tests run as root.
2. Add a private dependency seam at the scan/discovery boundary if necessary. Use real parser/files in ordinary tests; inject errors/counters only at the boundary under test. Do not use mutable package globals or change exported production options just for fault injection.
3. Repair the large-rollout test's missing-state handling: `Load` returning nil must give a clear test failure, not a nil dereference.
4. Replace the symlink to an active source with a private snapshot. Copy with a bounded buffer into a mode-0700 private directory, with mode-0600 files. Compare source identity/size/mtime before and after copying and fail preparation on change. Do not retry forever or alter the live source. Prepare outside the measured worker; remove private artifacts on cleanup. Choose a disk-backed scratch location if `/tmp` is tmpfs.
5. Keep a private copied state. Deep-copy slices/maps or construct it explicitly. Clear its hook queue so the probe cannot parse unrelated private files. Preserve processed IDs and pending-score environments needed by the particular case. Set controlled snapshot mtime and scan time, and assert eligibility before scanning. Never write the supplied state or reset production progress.
6. Create a positive oracle in the **same snapshot**: append a complete, unique synthetic turn using the existing valid fixture structure. Its trace must be absent from processed/pending state. Require exactly one span and one score for that synthetic trace and the expected input/output in stub callbacks. A separate small marker file is insufficient: the large target itself could still be skipped.
7. Prepare expected original-source turns/checkpoints before measurement, using the parser plus existing golden-contract checks as the projection oracle. Count unique source traces, source traces intersecting processed IDs, pending-score source traces, and expected original span/score calls. Report those distinct counts. Do not call the size of the whole state's processed list “recognized source traces.” If preparation cannot fit the declared safe resource envelope, record the real-input check as blocked; do not silently replace the source.
8. In the measured worker, invoke the real watcher/parser with local callbacks. Assert original expected callbacks, synthetic callbacks, no processed-trace spans, pending-score-only calls, final processed/pending state, and the watermark. Require parser invocation for the target and include-predicate visits for known first/last original trace IDs. Do not rely only on a watermark or an “opened file” counter.
9. Add negative controls: deliberately exclude the target by mtime; deliberately skip its parser; append malformed JSON after the marker. Each must fail the success oracle or produce the specified incomplete-scan outcome. Run these as expected-failure assertions or disposable local mutations, never as permanently failing suite tests.
10. Keep the live test opt-in. Missing live inputs may skip ordinary CI; a skipped live test is not release acceptance. Add a small generated equivalent in the default suite so oracle correctness is always tested.

**Exit:** the probe cannot pass without consuming its large target; fault regressions fail on the old behavior for the intended reason. Record failing-test output before the corresponding production fix. Preparation memory is excluded from worker RSS, but its own safety is still monitored.

### W2 — Consistent discovery, progress and health

1. Change `SessionPaths` to return `([]string, error)` and use one recursive `WalkDir`. Collect matching files at all depths in deterministic sorted order. Continue readable sibling subtrees after an error and return partial results with aggregated errors. Do not follow directory symlinks; retain matching file-symlink behavior. Include walk/root errors; never replace them with an empty successful list.
2. Update every caller found by `rg 'SessionPaths\('`. The watcher uses partial paths and marks discovery incomplete. `FindSessionByID` and `LatestSession` return discovery errors instead of asserting a unique/latest answer from an incomplete inventory. Preserve behavior for a complete empty inventory and ambiguous session IDs.
3. Capture the starting watermark and scan time once. Keep the existing eligibility interval `(watermark, scanStarted]`. Future-mtime files are deferred normally, not permanently processed. A stat error makes the scan incomplete. A parser error does likewise; never export a valid prefix as though the corrupt file parsed successfully.
4. Continue processing readable eligible files after source failures. Preserve successful per-turn checkpoints. Advance the global watermark only after complete discovery and successful handling of all eligible candidates. State writes must still reload under the existing sidecar lock; do not save a stale scan-wide state snapshot.
5. Emit one bounded aggregate line per incomplete scan, including quiet mode: `ERROR: watch_scan_incomplete discovery_errors=N stat_errors=N parse_errors=N delivery_errors=N changed_sources=N watermark_advanced=false`. Use `changed_sources` for a source changed during handling without an I/O error; use `stat_errors` for a failed post-parse stat. Do not include prompt text, tool payloads or credentials. Existing detailed path/error warnings may remain when not quiet. Backoff in W3 must not erase failure counts.
6. Keep source errors recoverable inside the watcher. Return fatal state errors and cancellation as before. Do not restart to retry a corrupt source; do not turn an export/checkpoint failure into successful progress.
7. Extend doctor tests to show the new marker causes `recent_errors=fail` and a nonzero doctor exit. Its existing 15-minute journal window is deliberately conservative: after recovery, doctor can remain failed until the last error ages out. Document that this is recent failure history, not an instantaneous scan-state API.
8. Make inability to read the journal a failed/unavailable health check, not an overall green result with missing scan evidence. Do not claim doctor proves that a currently running long scan has completed. Deployment acceptance separately requires observed completed scans and the automatic canary.
9. Add a distinct doctor failure for nonempty pending scores, including the count but no IDs. Preserve existing queue/state checks. An in-flight turn may transiently fail readiness; deployment checks wait for it to settle without deleting entries.

**Exit:** default regressions prove old-mtime stat recovery, partial discovery with healthy progress, parse failure with healthy progress, quiet error visibility, honest doctor failure/unavailability and existing checkpoint behavior.

### W3 — Avoid redundant work without changing durable state

1. Introduce a private `scanRuntime` shared by `WatchSessions`; `ScanOnce` creates a fresh instance for one call. A new watcher process starts empty. Test seams and fake clock belong to this private layer.
2. Store at most 1,024 per-path metadata entries, with LRU eviction. This is a cache capacity, not a limit on sources scanned/exported. Eviction causes normal work on the next eligible scan. Store no turns, observations, input/output text or decoded JSON in entries.
3. A successful entry requires a successful parse, no failed actionable turn, and successful post-parse stat with the same file identity (`os.SameFile`), size and nanosecond mtime as before parsing. If the source changed or post-stat failed, do not cache success and do not advance the watermark past uncertain work. Already committed turns remain committed.
4. On each scan, still discover and stat sources. Bypass parsing only when an eligible source's successful fingerprint is unchanged. Cache entries are never used to bypass discovery/stat errors or the ordinary mtime interval. Successful empty, processed-only or currently incomplete sources may be cached after stable parsing; later appends must invalidate the fingerprint.
5. Do not cache success when any selected trace still needs a score/checkpoint. A score retry must use its persisted environment and send zero spans. After a successful retry, update the processed-ID membership set before handling subsequent sources in that scan.
6. Track failed parse attempts separately in the same bounded metadata cache. For an unchanged fingerprint, retry no more often than 30 seconds; changed identity/size/mtime retries immediately at the next poll. For stat/discovery errors retry normal discovery/stat each poll because no trustworthy fingerprint exists. No added background goroutines or CLI policy knobs.
7. A deferred parse retry is still an incomplete scan, pins the watermark and contributes to `parse_errors`. Healthy/new sources run every poll. The retry is due at the first poll on/after its deadline; do not sleep 30 seconds inside a scan.
8. Evicting a failure entry must cause another real attempt, not mark it recovered. A complete successful discovery that no longer contains a removed path may cease reporting it; document the source-removal/old-mtime restoration limit in section 2. Never mutate/delete a user's source to clear health.
9. Check context cancellation between sources and before waits. Preserve existing state-lock retry logic; this cache does not retry network sends or create a second checkpoint mechanism.

**Exit:** repeated scans parse an unchanged healthy source once while corruption remains; changed/new sources still progress; repair unblocks progress; restart and eviction cause safe rereads; pending scores are not suppressed. All tests use fake scan time and counters, not sleeps or scheduler-sensitive timing assertions.

### W4 — Measure memory and define the supported envelope

This phase validates the streaming/filtering mechanism and resource envelope. Do not claim arbitrary-size bounded memory. Follow ADR-PERF-001: ordinary unit correctness must not depend on microsecond deadlines or one noisy benchmark sample.

1. Add a parser mechanism regression. Route retained-turn/observation construction through a private, normally nil test observer or an equivalent narrow internal helper, and assert rejected processed IDs never reach retention construction. Preserve actual parsing and projection assertions; a fake parser is not proof. Keep any observer private, passed explicitly to the private parse helper, and allocation-free when absent. No mutable global observers or production API/configuration surface.
2. Prove this regression catches a disposable mutation that moves filtering after full parsing. Separately compare `ParseTurnsFiltered` selected output with `ParseTurns` plus selection on small mixed/interleaved fixtures. Preserve tool pairs, usage, completion, redaction and trace IDs.
3. Generate the matrix below deterministically with streaming writes before worker startup. Build one test binary, then run a fresh child process for each case. Workers read prepared inputs, call the normal scan with stubs, assert exact expected effects, and emit a small machine-readable summary. No fixture generation, snapshot copying, full oracle preparation or compiler memory inside the RSS measurement.
4. Capture worker maximum RSS with `/usr/bin/time -v` (KiB on this Linux host), elapsed time, source bytes, maximum record bytes, source/selected/processed turn counts, span/score counts and final state counts. Record Go version, GOOS/GOARCH, GOMAXPROCS and revision. Do not use race-instrumented RSS as a production budget measurement.
5. Run each case five times serially. Report all values and worst-case RSS; do not choose the best result. Compare the same workload on the deployed baseline and candidate. Keep baseline measurements separate from the candidate acceptance decision.
6. Add an opt-in watcher memory gate that fails if worker exit/oracle fails or RSS exceeds the previously recorded absolute allowance. Keep the numbers and workload rationale in this plan/TESTING, with one implementation source for test constants; do not add a second fixture manifest. No automatic retries or host-load skips to convert a failure into a pass.
7. Microbenchmarks may report allocations and CPU cost without binding timers. Resource limits apply to the declared scan workloads. Confirm the retention mechanism with the regression plus measured results; neither final output counts alone nor a single RSS sample proves it.

| Case | Synthetic construction | Required behavior |
| --- | --- | --- |
| P100 | At least 100 MiB processed history, many ordinary records, one new complete turn at EOF | One new span/score; processed turns never retained |
| P400 | Same maximum record/selected turn sizes as P100, at least 400 MiB processed history | Same output contract; quantify history-related overhead, including trace-ID metadata |
| U100 | At least 100 MiB with many unprocessed completed turns | Every expected turn exported/scored; no loss under backlog |
| T100 | One new completed turn totaling at least 100 MiB across ordinary records | Full existing projection preserved; quantify retained-turn memory |
| R16 | One valid record with at least 16 MiB payload inside a selected completed turn | No scanner token-limit failure or silent truncation; maximum-record allocation visible |
| S100 | P100-sized history plus a persisted pending-score turn | No span for pending turn, exactly its score retry, persisted environment preserved |
| C100 | P100-sized healthy source plus corrupt eligible sibling, several scans | Healthy source parsed once per stable fingerprint; corruption remains degraded; repair and new turn recover |
| Real snapshot | W1 stable private incident-like source with a synthetic EOF marker | Actual source consumption and overlap proven; exact callbacks/state; local-only execution |

Numbers above define proposed test inputs, not runtime caps. Use complete valid records to reach at least the requested byte size and record actual size. If the measured machine cannot safely run a case, mark the gate blocked and use an isolated machine with enough capacity; never shrink the case silently or risk the active host's workloads.

**Resource protocol to fill in W0, before candidate measurements:**

| Field | Value / evidence |
| --- | --- |
| Intended concurrent workload and host | Single watcher on this 30.5 GiB host; 17:38 snapshot included active Codex, Next.js and ClickHouse workloads |
| Baseline revision and toolchain | Installed `03139fa58eb57c9a66b24c76903847d9c1330ea3`, SHA-256 `9433a8c1ea7c944f47de0bee9def162a06f6605c79a566820788b67eb4dd23df`, Go 1.26.0; source baseline `14c656f` |
| Worker memory allowance | 768 MiB hard systemd cgroup `MemoryMax` and `/usr/bin/time` maximum process RSS per isolated worker; 1.48 times observed installed watcher peak of 519.7 MiB; worker swap disabled |
| Host memory reserve and measurement period | `MemAvailable >= 2 GiB`, rounded up from the largest sampled Codex process at 1.96 GiB; 60 minutes, one sample per minute |
| PSI/paging limits and sampling cadence | PSI some `avg10 <= 5.0`, full `avg10 <= 1.0` each minute; record `pswpin`/`pswpout` deltas; no OOM/restart or unexplained sustained paging/pressure |
| Protocol fixed before candidate resource measurements | W0 update in this execution record, 2026-09-22; 768 MiB hard cgroup cap plus measured process RSS and no swap |

**Failure rule:** if U100, T100 or R16 cannot meet a justified allowance without semantic changes, leave memory acceptance open and document the measured cause. Do not automatically implement a streaming turn iterator, disk spool, record cap or lossy summary under this plan. A projection/storage design review is then required. The discovery/health fix may be reported separately; it must not be called full resource acceptance.

**2026-09-23 user override:** the user explicitly authorized running the corrected matrix on this host and accepted possible data loss. The matrix below ran in 768 MiB no-swap worker scopes. The user waived the host PSI/reserve guard for this run; its observed breaches and OOM outcomes remain recorded, and this waiver is not evidence that the host met the original environmental limits.

**Exit:** all functional/mechanism assertions pass, the complete declared matrix has measured results, and candidate resource results satisfy the predeclared allowance. Unset criteria, skipped cases or an over-budget result block this phase's acceptance.

### W5 — Regression, documentation and review

1. Update `README.md` with conservative watermark behavior, corrupt-source retries, recent-error doctor semantics, missing-root behavior, cache/restart behavior, and mtime recovery limits. Explain troubleshooting without recommending state deletion, broad process stops or manual canary replay.
2. Update `TESTING.md` with actual implemented test names and the isolated worker commands. Clearly distinguish default correctness, resource acceptance, optional private-input probes, and live receiver checks. Register no unimplemented test as a passed gate.
3. Run the full Production Gate in `TESTING.md`, including the full suite, coverage, both existing fuzz targets, provider/state checks, and five serial watcher latency checks. Then run targeted race coverage:

   ```sh
   go test ./... -count=1
   go test -race ./internal/codextrace ./internal/watch ./internal/exportstate ./internal/claudehook ./cmd/codex-langfuse-exporter -count=1
   go test -p=1 ./internal/watch -run '^(TestEvalWatchExportLatency|TestEvalHookQueueDrainLatency)$' -parallel=1 -count=5 -v
   git diff --check
   ```

4. Repeat new deterministic failure/cache tests five times. Use `go test -list` to verify exact targeted names exist; a regex selecting no tests is not a pass. Keep timeout/readiness safeguards on subprocess tests. No runtime mutation should be left from negative-control checks.
5. Review state writes for concurrency: pending-score, processed IDs and queued hooks must survive concurrent transactions. Review all early returns for watermark/cancellation correctness. Confirm the cache retains metadata only and cannot suppress uncompleted durable work.
6. Record any failures, their cause, correction and rerun. A changed assertion must still enforce the original test purpose. Check the public diff for secret/private data and temporary artifacts.

**Exit:** all executed local gates green, W4 acceptance status explicit, and documentation matches implementation. No deployment is implied by this exit.

### W6 — Publish, install and accept the runtime

Carry this phase out when implementing the plan under the user's deployment authorization. Writing this plan itself does not install a new binary.

1. Commit reviewed code/tests/docs and push the tested source to GitHub `main` using the repository's normal workflow. Verify local HEAD and remote main match; do not force-push or overwrite unrelated work. If commits change after testing, rerun affected gates and record the exact source actually released.
2. Record the prior running build and SHA-256 as a rollback artifact. Use the existing `./install.sh` staged preflight/cutover; do not replace its lock upgrade logic. There is no state migration. Never remove/reset the state or sidecar to pass install/doctor checks.
3. Verify build metadata (`go version -m`), installed executable SHA-256, and the executable of the current systemd `MainPID` match the intended artifact. Require `vcs.modified=false`, active/running/enabled user service, correct unit ExecStart, version 3 state, and preserved progress/queue. Do not reuse a historical PID from this plan.
4. Complete at least 60 minutes of observation under the predeclared host protocol. Record timestamps, restarts, kernel/service OOM evidence, completed scan progress, RSS, MemAvailable, PSI and swap-in/out deltas with units. Require no new restart/OOM, sustained service health and the declared resource/host limits. Full swap alone neither proves current pressure nor demonstrates reserve; assess it using the fixed workload protocol.
5. Check doctor after the 15-minute recent-error window has settled, and require no unexplained pending-score/queue backlog. Confirm incomplete scans are not being hidden by quiet mode. Do not corrupt a real source to test health in production; use W1-W3 tests for fault injection.
6. Create one fresh benign Codex turn through the existing automatic watcher path. Observe export-success, checkpoint/exported and scored diagnostics. Run the existing read-only `TestLiveCodexSmokeTrace` for that trace, requiring one root, one transcript and unique stable observation IDs. Never manually re-export the same canary.
7. Run Claude CHECK-001 only on an appropriately configured host with Claude and its installed Stop hook. If unavailable, explicitly mark it unperformed; local hook tests and a Codex canary do not establish live Claude parity. Do not edit Claude settings or install a new polling path.
8. Update the canonical handoff and this plan with exact source, published revision, binary digest, running identity, state counts, commands, observation window, canary outcome and remaining limitations. Documentation-only commits after runtime publication must be identified separately from the installed build.

**Rollback:** use the recorded previous streaming/advisory-lock binary (historical build `03139fa` is a reference, not an assumed current artifact). Preserve current v3 state and sidecar, and use the installer/cutover procedure. Do not restore an old state backup, resurrect the exclusive-create lock protocol, or roll back to whole-file parsing to address a new defect. Recheck running identity, doctor, state and automatic delivery. Rollback restores the old scan/health limitations and does not remove the acceptance/checkpoint replay window.

**Exit:** exact published/installed/running identities match; local and resource gates pass; the predeclared live window and new automatic canary pass. Report Claude unavailability or unresolved host-capacity acceptance explicitly rather than converting it into a pass.

## 5. Required regression matrix

Use these names or document their final exact replacements in TESTING. Do not create a second fixture registry for this table.

| Proposed test | Essential assertion |
| --- | --- |
| `TestSessionPathsIncludesMixedDepths` | Shallow and YYYY/MM/DD matches both returned, once, sorted |
| `TestSessionPathsReturnsPartialResultsAndError` | Healthy sibling returned; injected/root error not swallowed |
| `TestWatchStatFailureRetainsWatermarkAndRecovers` | Failed stat holds watermark; recovery at unchanged eligible mtime exports once, including after new runtime creation |
| `TestWatchDiscoveryFailureAllowsHealthyProgress` | Partial walk error holds watermark while healthy trace checkpoints |
| `TestWatchMissingSessionsRootIsIncompleteAndDrainsQueue` | Missing root holds progress, reports the error and still drains an already queued hook request |
| `TestWatchCorruptSourceDoesNotBlockHealthyTurn` | Corrupt file yields no prefix export; healthy trace checkpoints; watermark stays old and unchanged healthy source is parsed once |
| Quiet incomplete-scan assertions in stat, discovery, and corrupt-source regressions | Aggregate error survives quiet mode with category counts and no source path/content |
| `TestDoctorMode` | Incomplete-scan marker, pending scores, and unavailable journal each produce a failing doctor result |
| `TestWatchChangedDuringParseDoesNotCacheOrAdvance` | Post-parse fingerprint change blocks watermark/cache success; next scan finds and exports appended turn |
| `TestWatchParseRetryDeadlineAndRepair` | Unchanged failure deferred until 30-second deadline but still degraded; changed source retries next poll |
| `TestWatchCacheEvictionRereadsWithoutRepeatingCompletedWork`, `TestWatchRestartDoesNotDuplicateDurablyProcessedTrace`, and `TestWatchScanCacheEvictsOldestMetadataEntry` | >1,024 sources are handled; eviction/restart may add reads but never loses work or repeats completed span sends |
| `TestWatchCachedIncompleteTurnCompletesAfterAppend` | Incomplete turn is not falsely finalized; later append invalidates cache and completes normally |
| `TestWatchPendingScoreBypassesSuccessCache` | Failed score retried with stable environment and zero extra spans |
| `TestLargeRolloutTargetConsumptionOracle` and `TestLargeRolloutProbeRejectsSkippedTarget` | Skipped/incomplete target fails oracle even if watermark advances |
| `TestLargeRolloutProbeRejectsMalformedTail` | Corrupt EOF fails complete-scan oracle |
| `TestParseTurnsFilteredOmitsProcessedTurnObservations` | Reject-before-retention mechanism proven, and filtered output matches full parse followed by selection |
| Existing delivery/lock/hook/golden tests | Contracts and concurrency unchanged |

## 6. Execution record

Do not check a box because the code compiles or a related older test passed.

- [x] W0 baseline and acceptance protocol
- [x] W1 registered reproductions and non-vacuous probe
- [x] W2 discovery/progress/health semantics
- [x] W3 disposable metadata cache and retry scheduling
- [x] W4 candidate/baseline memory matrix passed; host-pressure guard waived by user
- [x] W5 full local gate and documentation review
- [ ] W6 publication, installation and scoped live acceptance

| Phase / date | Source revision | Commands and actual result | Trouble / correction | Risk and operator follow-up |
| --- | --- | --- | --- | --- |
| W0, 2026-09-22 17:38 PDT | Source `14c656f`; runtime build `03139fa` | `go test ./... -count=1` PASS before code edits; `git diff --check` PASS; installed/running binary identities and state v3 counts verified. Active/enabled PID `1181025`, invocation `31dc3d79b4de49119414a4fed025c018`, zero restarts. Host and service memory evidence recorded above. | Initial W1 path test caught shallow-glob omission; the new partial-walk test needed to model `SkipDir` after injecting a directory error. | W4 candidate measurements must stay in isolated worker cgroups at or below 768 MiB; W6 must meet the fixed 2 GiB reserve/PSI protocol. |
| W1, 2026-09-22 | Candidate worktree from `14c656f` | `go test ./internal/watch -run '^(TestLargeRolloutProbeRejectsSkippedTarget|TestLargeRolloutProbeRejectsMalformedTail|TestLargeRolloutTargetConsumptionOracle)$' -count=1 -v` PASS for the default oracle controls. Separately, the private-input `TestLiveCodexLargeRolloutFilteredScan` ran under `MemoryMax=1G`, `MemorySwapMax=0`: source 275,378,826 bytes, 59 source trace IDs, 58 already-processed overlap, one EOF marker visit, one span and one score, parser calls=1, max RSS 224,920 KiB, 27.94 s wall time. Its original source and v3 state were read-only; it copied the source to private disk-backed `/var/tmp`. | The first oracle draft had an unnecessary marker-ID membership assertion; replaced with an explicit positive control plus missing-target, partial-target, and missing-EOF negative controls. | During the real probe `MemAvailable` stayed above 2 GiB, but one PSI sample exceeded the fixed avg10 thresholds (`some=6.87`, `full=4.26`); pressure fell after the test but was still not fully below threshold in the last sample (`some=1.73`, `full=1.34`). W6 requires a quiet 60-minute window and no PSI threshold breaches. |
| W2, 2026-09-22 | Candidate worktree from `14c656f` | `go test ./internal/codextrace -run '^(TestSessionPathsIncludesMixedDepths|TestSessionPathsReturnsPartialResultsAndError|TestSessionPathsMissingRootIsAnError|TestFindSessionByIDPropagatesMissingRoot|TestLatestSessionPropagatesMissingRoot|TestParseTurnsFilteredOmitsProcessedTurnObservations)$' -count=1` PASS; the exact watcher regression command in TESTING PASS; `go test ./cmd/codex-langfuse-exporter -run '^TestDoctorMode$' -count=1` PASS. The watcher diagnostic now reports two injected underlying discovery errors, not only one failed discovery call. | The old-mtime recovery fixture uses a broken file symlink, then restores its target with the same eligible mtime; it proves a fresh one-shot runtime retries after a stat failure without changing state schema. | Missing `sessions/` now remains a visible degraded condition even before Codex creates that directory. Operator must diagnose the path/host state, not reset v3 progress. |
| W3, 2026-09-22 | Candidate worktree from `14c656f` | `go test ./internal/watch -run '^(TestWatchStatFailureRetainsWatermarkAndRecovers|TestWatchDiscoveryFailureAllowsHealthyProgress|TestWatchCancellationAfterParseDoesNotExportOrAdvance|TestWatchCorruptSourceDoesNotBlockHealthyTurn|TestWatchParseRetryDeadlineAndRepair|TestWatchCachedIncompleteTurnCompletesAfterAppend|TestWatchPendingScoreBypassesSuccessCache|TestWatchChangedDuringParseDoesNotCacheOrAdvance|TestWatchScanCacheEvictsOldestMetadataEntry|TestWatchCacheEvictionRereadsWithoutRepeatingCompletedWork|TestWatchRestartDoesNotDuplicateDurablyProcessedTrace|TestWatchMissingSessionsRootIsIncompleteAndDrainsQueue|TestWatchFiltersProcessedTurnsBeforeRetainingObservations|TestLargeRolloutProbeRejectsSkippedTarget|TestLargeRolloutTargetConsumptionOracle|TestLargeRolloutProbeRejectsMalformedTail)$' -count=1` PASS. This also compiled the new comparative-baseline worker and resource-gate files. | The first compile after adding the baseline worker caught a value-versus-pointer state assignment; the helper now keeps `ScanOnce`'s value result and this rerun passed. A 1,026-entry eligible working set exceeds the 1,024-entry LRU. On the next pinned scan, normal LRU churn can reparse all sources; assertions require safe bounded rereads and zero duplicate span callbacks, not an unrealistic claim of one parse for an over-capacity set. | The cache limits metadata memory, not source count. If real deployments commonly keep more than 1,024 eligible sources behind a persistent failure, measure scan cost; consider a separate durable recovery design only if that real workload warrants it. |
| W4 preliminary, 2026-09-22 | Candidate worktree from `14c656f`; installed comparison still pending | `CODEX_LANGFUSE_RUN_MEMORY_GATE=1 go test ./internal/watch -run '^TestWatchMemoryEnvelope$' -count=1 -timeout=90m -v` PASS in 95.553 s. All seven cases ran five serial workers. Per-case source bytes and `/usr/bin/time` max-RSS samples (KiB): P100 `104952933` `[32428,32292,33016,32976,32496]`; P400 `419797563` `[73424,73036,73172,73264,72736]`; U100 `105231345` `[197112,188728,194296,188152,199800]`; T100 `104868865` `[188216,184952,171124,175084,185876]`; R16 `16777978` `[65784,65656,68520,98616,67220]`; S100 `104953520` `[33112,33324,33364,32988,33640]`; C100 `104952933` `[32732,33104,32104,32680,32572]`. All measured process RSS values were below 768 MiB. | Re-reading the frozen protocol after the run showed the worker command used `MemoryMax=1G`, while W0 had fixed a 768 MiB hard cgroup limit. The run is therefore preliminary even though measured RSS passed. The test now uses `MemoryMax=768M`; candidate measurements must be repeated under the frozen cap. Same-workload measurement against the exact installed baseline and one-minute host PSI/reserve sampling were also not captured. | The host later fell below its reserve (`MemAvailable=1,478,820 KiB`) and PSI rose (`some avg10=27.08`, `full avg10=16.95`; later sample `some=43.50`, `full=25.33`), with swap counters increasing. Active Codex, Next.js, and ClickHouse workloads were left running. Do not rerun resource tests or deploy until a fresh sample is above 2 GiB and below the fixed PSI thresholds; capture the full 60-minute observation before marking W6 complete. |
| W4 parser-mechanism negative control, 2026-09-22 | Disposable worktree at `14c656f`; no production source mutation | A temporary parser mutation invoked the observation-retention seam for a trace that the include predicate rejects, then preserved the normal skip and final selected-turn projection. `go test ./internal/codextrace -run '^TestParseTurnsFilteredOmitsProcessedTurnObservations$' -count=1` failed for the intended assertion: `processed trace ... reached observation retention 1 times`. The worktree was removed; primary worktree remains unchanged by the mutation. | The first attempt's output was truncated by the tool; the test was rerun with captured, concise output and the intended failure was confirmed. | This proves the assertion detects late observation retention without broadening the final output contract. It does not replace the comparative memory matrix. At the latest preflight, `MemAvailable=3,478,672 KiB`, PSI avg10 some/full `0.28/0.25`, and the installed baseline watcher remained active with `NRestarts=1`; the post-OOM 60-minute acceptance window has not begun. |
| W4 fixture-oracle correction, 2026-09-22 19:12 PDT | Candidate source `14c656ff5462ae43a65648a2ffa6c5d8f84647ad` with working changes | Corrected P100/P400 processed-history fixtures to append one completed, selected EOF turn; both now require one span, one score, all processed contexts rejected, and the expected selected trace. C100 inherits the same healthy source shape. Added expected trace/completion/projection checks to candidate and baseline workers, plus a normal small fixture test. `GOMAXPROCS=2 go test ./internal/watch -run '^TestMemoryGateProcessedHistoryFixtureSelectsEOFTurn$' -count=5` PASS; after this correction `go test ./... -count=1`, the targeted race suite, and full coverage run all PASS. | Review found both resource-matrix attempts had omitted this selected EOF turn, despite the W4 case table requiring it. Their P100/P400 RSS values are now explicitly disqualified from case acceptance. The 1 KiB fixture test validates construction and behavior but is not resource evidence. | Rebuild both candidate and baseline test binaries from the corrected helper files. The complete 100/400 MiB matrix is still required, and the host PSI breach remains an independent blocker. Do not check off W4 using historical partial results. |
| W4 comparative matrix, interrupted 2026-09-22 18:43-18:45 PDT | Candidate source `14c656ff5462ae43a65648a2ffa6c5d8f84647ad` with working changes; installed baseline source `03139fa58eb57c9a66b24c76903847d9c1330ea3` | Built the candidate and baseline test binaries separately with Go 1.26.0 under 1 GiB no-swap build scopes. Baseline `go test -c` ran after an explicit `cd` into the detached baseline worktree; the worktree HEAD was printed and verified. Candidate SHA-256 `358bd013e87fabbbdc69ae6414b14b9eb15e3e748719ad09a91baed054c11dc5`; baseline SHA-256 after adding the identity guard `f88f4375a92182dc3b8d8238c59e1dce775723d1c548ca9c45702e4c79443c47`; each binary listed its expected gate test. The baseline binary now requires a linker-stamped exact source revision, and its identity check was verified before a fixture-less expected failure. Each completed worker used the same generated source/state pair, a 768 MiB `MemoryMax`, no swap and `/usr/bin/time` max-RSS measurement. Historical partial RSS results (KiB): P100 candidate `[31568,31808,31576,31332,32156]`, baseline `[32072,31404,31168,31436,30776]`; P400 candidate `[73252,71444,72216,71748,68888]`, baseline `[71572,71568,72248,73372,72152]`; U100 candidate `[192064,194368,193700,201044,196948]`. Source sizes were P100 `104952933`, P400 `419797563`, U100 `105231345` bytes. | The first baseline build attempt ran from the candidate checkout; identical hashes caught the mistake before any matrix sample. The baseline binary was rebuilt from the detached `03139fa` worktree, and a linker-stamped test guard was added so a future wrong-revision binary fails before measuring. During the matrix, the scheduled host sample at 18:44:52 recorded `MemAvailable=3475420 KiB`, PSI some/full avg10 `1.25/1.05`; a between-sample spot check reached `3.93/3.29`. The run was stopped after this fixed PSI breach. The first U100 baseline worker was interrupted (`SIGPIPE` after its parent stopped); its `187296 KiB` value is not a valid sample. | These P100/P400 source fixtures contained only processed turns and omitted the required new complete EOF turn. Their RSS values are not results for the declared P100/P400 cases; retain them only as historical diagnostics. U100 candidate measurements are complete but the baseline and T100/R16/S100/C100 remain incomplete. The host samples and swap counters are recorded without assigning cause. Rerun the corrected full matrix only on a sufficiently provisioned host with one-minute samples; do not accept the partial values. |
| W4 retry, interrupted 2026-09-22 18:57-18:58 PDT | Same candidate source; baseline test binary linker-stamped for `03139fa58eb57c9a66b24c76903847d9c1330ea3` | Rebuilt candidate SHA-256 `635adcf076b2a0c6beb646b8bb2d143c5c16727607222473d88b46426e8eddf5` and guarded baseline SHA-256 `5faec2e3202d0b89b96b087d07cb0e9c1d1177536f0d8d0b1c2da8e8491fde39`; expected test selectors were present. P100 candidate and baseline each completed five worker assertions: candidate RSS KiB `[31804,31460,31556,31812,32308]`, baseline `[31580,29948,30600,31772,31972]`. P400 candidate completed four samples `[72260,71948,72036,71956]`; the fifth was interrupted (`SIGPIPE`) and excluded. | At 18:57:31 the host sample was `MemAvailable=3690852 KiB`, PSI some/full avg10 `0.14/0.14`. During P400, 18:58:31 recorded `MemAvailable=3403788 KiB`, PSI `1.90/1.59`, again above the fixed full-pressure limit. The gate was stopped; no worker remained. | Self-review later found these P100/P400 fixtures also omitted the required new completed EOF turn, so those RSS values do not match the declared cases and are retained only as diagnostic history. The repeated PSI breach is an independent environmental blocker, not proof the candidate caused global pressure. The installed baseline service stayed active at `NRestarts=1`. Scratch was removed; do not retry P400 here. Complete the corrected matrix on a separate sufficiently provisioned host or leave W4/W6 blocked. |
| W5 local verification, 2026-09-22 | Candidate source `14c656ff5462ae43a65648a2ffa6c5d8f84647ad` with working changes | After the fixture-oracle correction, exact `go test ./... -count=1` PASS; full coverage PASS (watch 52.6%, codextrace 25.3%, CLI 58.7%, no aggregate threshold); targeted race suite for codextrace/watch/exportstate/claudehook/CLI PASS. Both codextrace fuzz targets PASS at 10s/one worker. Claude parser/hook/state checks PASS. Serial watcher latency suite passed 5 repetitions (max scan wall samples 1.96, 1.76, 1.54, 1.56, 2.59 ms; queue-drain test passed each time). Exact new regression selectors were confirmed with `go test -list`; discovery/parser, watcher/large-probe and doctor regressions passed `-count=5`; the new small P100 EOF fixture oracle passed `-count=5`. | The first full-suite run failed only `TestWatchRetriesQueueRemovalAfterCheckpoint`: the test expected watermark advancement but had no `sessions/` directory. Adding the empty directory represented an available but empty Codex root; two later full-suite runs and the post-fixture-correction run passed. Queue callbacks, lock contention, checkpoint, and watermark assertions were unchanged. | Local execution gates passed. The final review below closes W5; W4 resource acceptance remains unresolved, so W5 does not authorize publication or installation. |
| W5 closeout / W4-W6 handoff, 2026-09-22 19:21 PDT | Candidate source `14c656ff5462ae43a65648a2ffa6c5d8f84647ad` with working changes | `gofmt -d` over all changed Go files produced no output; `git diff --check` PASS; secret-pattern scan PASS across all 18 changed/new files. Reviewed discovery error propagation, parser retention seam, scan watermark/cache/pending-score paths, doctor health checks, memory-gate assertions, and user-facing docs. W5's full local suite, coverage, race, fuzz, repeated regression and latency results are recorded immediately above. | No new code changes were needed during final review. | W4's corrected worker matrix is now complete; see the 2026-09-23 result and host-risk record below. The host-pressure acceptance is waived by the user only for this matrix. W6 still requires exact release identity, preserved state, a new 60-minute post-install observation and a fresh automatic canary. |
| W4/W6 operational hold, 2026-09-22 18:27 PDT | Installed baseline runtime `03139fa`; candidate not installed | Read-only service/kernel journal review found the installed watcher was OOM-killed during a global host OOM event; systemd automatically restarted it (`NRestarts=1`). At 18:28 the new invocation was active, with about 70 MiB cgroup current use; host `MemAvailable=3,460,040 KiB`, PSI `some avg10=8.89`, `full avg10=5.33`, and swap counters had advanced. The service journal recorded a 519.6 MiB cgroup peak and 3.6 MiB swap peak for the failed run. At 18:33, the installed executable and new `/proc/834628/exe` both hashed to the known baseline digest `9433a8c1ea7c944f47de0bee9def162a06f6605c79a566820788b67eb4dd23df`; no candidate was running. | The OOM was not caused by a candidate install or intentional service action. Kernel evidence shows multiple concurrent global-pressure victims; it does not establish one root cause. No unrelated process was stopped and no host policy or state was changed. | This resets any no-OOM observation. The candidate resource matrix, full suite, publication/install and new canary remain unrun. Wait for `MemAvailable >= 2 GiB` and both PSI limits before resource-heavy checks; require a fresh 60-minute host/service window with no restart/OOM before deployment acceptance. |
| W4 same-host comparative matrix, 2026-09-23 05:47-05:57 PDT | Candidate binary based on source `14c656ff5462ae43a65648a2ffa6c5d8f84647ad` plus working changes; baseline source `03139fa58eb57c9a66b24c76903847d9c1330ea3` | `CODEX_LANGFUSE_MEMORY_GATE_BASELINE_BINARY=/var/tmp/codex-langfuse-baseline-03139fa.test CODEX_LANGFUSE_RUN_MEMORY_GATE=1 GOMAXPROCS=2 go test -p=1 ./internal/watch -run '^TestWatchMemoryEnvelope$' -count=1 -timeout=90m -v` PASS in 568.328 s. Candidate binary SHA-256 `34d7338ec0bd314223eb9f59b76200f886ad3f720265626c62bdce4b8b7cea9e`; baseline SHA-256 `2e49d5818c8444926642b50d0bc3f6a6eefb035fde43cb4094ac2903ab5efca3`, linker-guarded for the exact baseline revision. All seven cases ran five serial samples per revision. Every worker passed its functional oracle and stayed below the 768 MiB process-RSS allowance; detailed vectors follow this execution table. | On the user's explicit direction, the run proceeded on the same host despite the earlier no-retry recommendation. PSI later peaked at avg10 some/full `68.75/41.33`; `MemAvailable` fell to `1,432,568 KiB`. The kernel recorded global OOM kills during the run, including the watcher at 05:55:32. systemd restarted the watcher at 05:55:38 (`NRestarts=1`). Kernel records also show unrelated browser, gateway, SSH-worker, desktop-session and CI-runner processes killed. The run is temporally associated with host pressure; the records do not establish one initiating process. No unrelated service was manually restarted. | Candidate worker RSS and all case assertions passed. The user waived the predeclared host PSI/reserve guard for this measurement, so these data do not certify host-capacity safety. Preserve the waiver and OOM evidence. Before W6 acceptance, verify the restarted baseline state and service, then install only through the staged installer, monitor the candidate for a fresh 60-minute window, and check for any further OOM/restarts and unexplained state backlog. |
| W6 candidate cutover / initial observation, 2026-09-23 06:05-06:10 PDT | Published commit `db270362f898e6be2e37220d8aefe7fdf517cd11`; prior running binary SHA-256 `9433a8c1ea7c944f47de0bee9def162a06f6605c79a566820788b67eb4dd23df` | `git push origin main` PASS and local/remote `main` match. `./install.sh` PASS. Installed and `/proc/459013/exe` SHA-256 both `7d638c452c382828b0146c82b0382db156dd3d0c1e9cb63e6ecc5d96c7cb1389`; `go version -m` reports revision `db270362f898e6be2e37220d8aefe7fdf517cd11`, `vcs.modified=false`, Go 1.26.0, linux/amd64. User service is enabled, active/running, ExecStart is the installed exporter, and post-cutover `NRestarts=0`. State v3 is preserved with processed count advancing 1,057→1,059, queue=0 and pending scores=0. Kernel query found no OOM lines since the candidate start at 06:05:41. | Doctor config, health, auth, project, state and watcher checks pass. Its `recent_errors` check fails from `watch_scan_incomplete` markers with only `changed_sources=1` or `2`; no discovery, stat, parse, delivery, checkpoint or queue errors were counted. The 15-minute error window is still settling while active source files change. PSI rose after install; at 06:09:53 avg10 some/full was `10.80/7.18`, `MemAvailable=2,760,184 KiB`, and `SwapFree=4 KiB`. | The post-install observation began at 06:05:41 PDT. Initial one-minute samples show the watcher active with zero restarts and state intact; they do not yet establish a 60-minute pass. Keep observing at one-minute cadence, record PSI/reserve and paging deltas, and check kernel OOM/restart evidence. Run doctor after the 15-minute recent-error window settles, then perform one fresh automatic canary and its read-only trace-shape check. |
| W6 early observation samples, 2026-09-23 06:10:54-06:15:58 PDT | Same published/installed revision `db270362f898e6be2e37220d8aefe7fdf517cd11`; PID `459013`; executable digest unchanged at `7d638c452c382828b0146c82b0382db156dd3d0c1e9cb63e6ecc5d96c7cb1389` | One-minute samples show service active with `NRestarts=0`, state v3 processed=1,060, queue=0, pending scores=0, and no kernel OOM lines found since 06:05:41. Across six samples, `MemAvailable` was 2,536,080-3,276,944 KiB (above the 2 GiB reserve); PSI avg10 ranged from 0.87 to 17.79 (`some`) and 0.71 to 13.98 (`full`), breaching the fixed `some <= 5.0` and `full <= 1.0` limits at multiple samples. Swap remained effectively full (0-152 KiB free); `/proc/vmstat` paging counters continued to increase. Service cgroup peak was 246,128,640 bytes (234.7 MiB). Recent journal error lines rose 15→30 in the rolling 15-minute window, all observed markers reported only `changed_sources=1-2`; no discovery/stat/parse/delivery category errors appeared in the reviewed excerpts. | Host-pressure guard is not passing even though reserve, service identity and state are intact. Repeated PSI breaches are measured facts; concurrent host workload is not attributed as a sole cause. The `recent_errors` doctor gate cannot pass while these changed-source markers remain in its 15-minute window. | Continue the already authorized same-host observation and collect one-minute samples through the full post-install window. Keep the original PSI thresholds unchanged in the record. Run the benign automatic canary when host pressure is lower; report host-capacity acceptance as failed if any breach occurs in the declared window. |

| W6 automatic Codex canary, 2026-09-23 06:20 PDT | Codex CLI `0.156.1`, configured model `gpt-6-luna` | A fresh benign `codex exec` turn completed and the installed watcher logged `span_export_succeeded`, `exported`, and `scored` at 06:20:21 PDT. The read-only `TestLiveCodexSmokeTrace` PASS showed root_count=1, transcript_count=1, observation_count=2, unique IDs, stable for 5 seconds. The trace ID and session ID are omitted from this public operational record. This live snapshot does not prove historical uniqueness or backend-wide exactly-once behavior. | Canary path and current trace shape pass. Claude CLI is unavailable, so Claude CHECK-001 is unperformed. | Retain the successful result; do not manually replay the canary. |
| W6 live doctor snapshot, 2026-09-23 06:25 PDT | Installed runtime source `db270362f898e6be2e37220d8aefe7fdf517cd11` | `/home/kirill/.codex/bin/codex-langfuse-exporter --doctor` exited 1. Config, Langfuse health/auth/project, state (`processed=1,065`, queue=0), and watcher checks passed; `recent_errors` failed with `count=43`. A path-free 15-minute journal aggregation found 44 `watch_scan_incomplete` lines: discovery/stat/parse/delivery sums were all 0 and `changed_sources` sum was 51. The service was active with `NRestarts=0`; kernel OOM query since 06:05:41 remained empty. | The doctor failure is consistent with the scan detecting source mutation during active Codex writes; the available journal fields show no I/O or delivery category errors. The live check intentionally counts incomplete scans as unhealthy. | Keep this as a real failed recent-health gate until the source stops changing and the 15-minute window clears; do not weaken doctor semantics. Re-run after the observation only if a fresh quiet window can be established. |
| W6 host-pressure interruption, 2026-09-23 06:31-06:32 PDT | Installed candidate source `db270362f898e6be2e37220d8aefe7fdf517cd11`; service PID `459013` | At 06:31:12, host `MemAvailable=1,585,388 KiB` (<2 GiB), PSI avg10 some/full=`76.22/51.33`, service active with `NRestarts=0`, state processed=1,066 with queue=0 and pending=0. Kernel journal at 06:31:14 recorded a global OOM kill of a Chrome process (anon RSS 161,284 KiB); at 06:31:49 it recorded another OOM-killer invocation. At 06:32:17, `MemAvailable=1,265,416 KiB`, PSI avg10 some/full=`86.84/56.16`; watcher remained active with zero restarts. | This invalidates the first W6 host-capacity observation. Kernel evidence identifies an OOM victim but does not establish the initiating workload. No unrelated process was stopped or restarted. | Continue low-impact observation as authorized. Start a fresh full 60-minute W6 host window only after `MemAvailable >= 2 GiB` and both predeclared PSI avg10 limits (`some <= 5.0`, `full <= 1.0`) recover. Keep the thresholds and canary evidence; do not claim a capacity pass from this interrupted window. |
| W6 host-pressure acceptance stopped, 2026-09-23 06:37-06:50 PDT | Installed source `db270362f898e6be2e37220d8aefe7fdf517cd11`; binary SHA-256 `7d638c452c382828b0146c82b0382db156dd3d0c1e9cb63e6ecc5d96c7cb1389` | The one-minute observer ran through sample 28 at 06:37:27 PDT, then was stopped at the user's request to reduce resource use. It did not complete 60 minutes. systemd recorded repeated `oom-kill` exits for the watcher beginning 06:32:52; the restart counter reached 14 by the 06:50 check. At 06:50 the unit was active/running, `MemoryMax=infinity`, and state v3 was intact (`processed=1,067`, queue=0, pending=0). Last captured host sample at 06:45 had `MemAvailable=1,389,732 KiB`, PSI avg10 some/full=`87.65/47.37`; earlier lows were 1,205,240 KiB and peaks reached 98.58/77.58. Other global OOM victims were also recorded. | The host-capacity gate failed. Repeated OOM kills restarted the production watcher; do not attribute the events solely to this change or worker RSS. No running Docker containers or task-owned test/canary/sampler process remained; the production watcher was left running as requested. | Resume only after the host has recovered to the frozen `MemAvailable >= 2 GiB`, PSI avg10 some<=5.0/full<=1.0 limits. Then begin a new uninterrupted 60-minute window, require zero OOM/restarts, run doctor after its 15-minute error window is quiet, and verify state/export health. Keep v3 state; do not weaken thresholds. |
### W4 measurements (RSS in KiB)

Both revisions used the same generated sources/state, Go 1.26.0, Linux amd64, `GOMAXPROCS=2`, fresh 768 MiB `MemoryMax` scopes, `MemorySwapMax=0`, and `/usr/bin/time`. P100/P400 contain processed history followed by the selected completed EOF turn. The candidate test binary SHA-256 is `34d7338ec0bd314223eb9f59b76200f886ad3f720265626c62bdce4b8b7cea9e`; the baseline test binary SHA-256 is `2e49d5818c8444926642b50d0bc3f6a6eefb035fde43cb4094ac2903ab5efca3`.

| Case | Source bytes / max record bytes | Candidate samples (KiB), max | Baseline samples (KiB), max |
| --- | ---: | --- | --- |
| P100 | 104,953,500 / 2,172 | `[33808, 32000, 32048, 32504, 35548]`, 35,548 (34.7 MiB) | `[27260, 26652, 28388, 29180, 29028]`, 29,180 (28.5 MiB) |
| P400 | 419,798,130 / 2,172 | `[77280, 84336, 74768, 78724, 75676]`, 84,336 (82.4 MiB) | `[72864, 75160, 73072, 75848, 72044]`, 75,848 (74.1 MiB) |
| U100 | 105,231,345 / 102,546 | `[195340, 190796, 190476, 203276, 188364]`, 203,276 (198.5 MiB) | `[199364, 192028, 183776, 196828, 181536]`, 199,364 (194.7 MiB) |
| T100 | 104,868,865 / 1,048,681 | `[172440, 183180, 184028, 175332, 177292]`, 184,028 (179.7 MiB) | `[168408, 176524, 175640, 187356, 175824]`, 187,356 (183.0 MiB) |
| R16 | 16,777,978 / 16,777,321 | `[82316, 98444, 98572, 98504, 98444]`, 98,572 (96.3 MiB) | `[97180, 97024, 97308, 97496, 97344]`, 97,496 (95.2 MiB) |
| S100 | 104,953,520 / 2,172 | `[31612, 32740, 32692, 32788, 33336]`, 33,336 (32.6 MiB) | `[31200, 30676, 31332, 26496, 30508]`, 31,332 (30.6 MiB) |
| C100 | 104,953,500 / 2,172 | `[32416, 31892, 34632, 34104, 33240]`, 34,632 (33.8 MiB) | `[23732, 31880, 30868, 32268, 30332]`, 32,268 (31.5 MiB) |

The complete verbose run was captured at `/var/tmp/codex-langfuse-w4-20260923.log`; keep the table above as the durable summary and remove temporary binaries/logs after release verification.

## 7. Executor prompt

> Implement `plans/watcher-scan-recovery-and-memory-validation-plan.md` phase by phase. Read repository instructions and source-of-truth docs first. Preserve version 3 state, advisory locking, existing projection, per-turn checkpoints and at-least-once delivery. Begin with failing regressions and the positive large-source probe; implement conservative discovery/stat/parse handling, honest doctor diagnostics and a disposable metadata cache. Freeze justified resource criteria before candidate measurements. Do not weaken tests, drop input, repurpose the hook queue, invent resource budgets, or add a new export path. Run the full required local/resource gates and record real outcomes after each phase. Publish/install under the existing authorization and verify exact running identity plus the declared live window and a fresh automatic canary. If a required budget cannot be justified or met, record the measured blocker and required design decision; do not check off that phase or claim full production completion. Update the canonical handoff, this execution record, troubles and operator follow-ups as you work.
